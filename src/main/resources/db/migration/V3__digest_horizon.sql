-- На сколько дней вперёд собирать утренний список.
--
-- not null default 1: у существующих семей поведение не меняется — до этой
-- миграции дайджест показывал открытые дела без ограничения по дате, и молчаливое
-- расширение горизонта было бы изменением того, что человек уже привык получать.
alter table family add column digest_horizon_days integer not null default 1;
